# Execution Validation - Verification Matrix

- **Domain:** execution_validation
- **Prefix:** EXE
- **Spec:** DIG L2 Block Specification
- **Total Requirements:** 9

| ID | Status | Summary | Verification Approach |
|----|--------|---------|----------------------|
| EXE-001 | implemented | validate_execution API | Integration: `tests/test_exe_001_validate_execution_api.rs` — 6 tests: empty block returns empty `ExecutionResult`; `fn(&L2Block, &ValidationConfig, &Bytes32)` signature matches NORMATIVE (compile-time function-pointer coercion); non-zero `header.total_fees` on empty body → `FeesMismatch` (EXE-006); non-zero `header.total_cost` → `CostMismatch` (EXE-007); determinism; accepts `dig_clvm::ValidationConfig::default()`. Full CLVM pipeline scope belongs to EXE-003. |
| EXE-002 | implemented | Puzzle Hash Verification | Integration: `tests/test_exe_002_puzzle_hash_verification.rs` — `verify_coin_spend_puzzle_hash` uses `clvm_utils::tree_hash_from_bytes`; accepts matching spends; rejects tampered `puzzle_reveal` with full error context (coin_id / expected / computed); empty-puzzle vs zero hash; multiple spends halt on first mismatch; parity with `clvm_utils` confirmed. Wired into `L2Block::validate_execution` before CLVM invocation. |
| EXE-003 | implemented | CLVM Execution via dig-clvm | Integration: `tests/test_exe_003_clvm_delegation.rs` — 11 tests. `map_clvm_validation_error` exhaustive mapping (PuzzleHashMismatch, SignatureFailed, CostExceeded, ConservationViolation, CoinNotFound, AlreadySpent, DoubleSpend, Clvm). `L2Block::validate_execution_with_context` signature + empty-body delegation path. Architectural lint: no `run_spendbundle` / `validate_clvm_and_signature` / `spendbundle_conditions` references in `src/`. |
| EXE-004 | implemented | Condition Parsing and Assertion Checking | Integration: `tests/test_exe_004_condition_parsing.rs` — 11 tests on `collect_pending_assertions_from_conditions` covering every block-level absolute + per-spend relative field of `OwnedSpendBundleConditions`, combined cases, empty case, `coin_id` tagging. Pass 1 / Pass 2 assertion checks (announcements, concurrent, self-assertions) delegated to dig-clvm per NORMATIVE Implementation Notes; rejection surfaces via `ValidationError::Clvm -> BlockError::ClvmExecutionFailed`. ASSERT_EPHEMERAL handled by STV-002 directly. |
| EXE-005 | implemented | BLS Aggregate Signature Verification | Integration: `tests/test_exe_005_bls_delegation.rs` — 4 tests: `SignatureFailed` mapping with bundle_index; `dig_clvm::BlsCache` reachable via dig-clvm (no dig-block mirror); architectural lint — `src/` has no direct `aggregate_verify` / `BlsCache` / `chia_bls::sign` calls (AGG_SIG variants fully delegated to dig-clvm / chia-consensus); `Signature` / `PublicKey` are `chia-bls` re-exports verbatim. |
| EXE-006 | implemented | Coin Conservation and Fee Consistency | Integration: `tests/test_exe_006_fee_consistency.rs` — 7 tests: conservation `ConservationViolation{input,output}` maps to `CoinMinting{removed,added}` (Chia Check 16); block-level `zero_fees_empty_block_passes` happy-path; `fee_mismatch_high_rejects` with header/computed context; bidirectional shape (header<computed and header>computed both surface as `FeesMismatch`); `computed_fees_start_at_zero`; reserve-fee failure maps to `ClvmExecutionFailed`; u64::MAX boundary. Chia parity Check 19 `INVALID_BLOCK_FEE_AMOUNT`. |
| EXE-007 | implemented | Cost Consistency Verification | Integration: `tests/test_exe_007_cost_consistency.rs` — 7 tests: `zero_cost_empty_block_passes` boundary; `cost_mismatch_high_rejects` with header/computed context; bidirectional shape; `CostMismatch` distinct from `FeesMismatch` and from per-bundle `ClvmCostExceeded`; `u64::MAX` round-trip; error Display surfaces both values for logs. Chia parity Check 9 `INVALID_BLOCK_COST`. |
| EXE-008 | implemented | ExecutionResult Output Type | Integration: `tests/test_exe_008_execution_result.rs` — 9 tests covering `Default` emptiness, all-fields-populated struct-literal construction (proves field visibility + type), additions as concrete `Coin`, removals as `Bytes32`, `PendingAssertion` passthrough, one-receipt-per-bundle, scalar totals, bincode round-trip, and `&ExecutionResult` borrow (simulates STV-001 handoff). |
| EXE-009 | implemented | PendingAssertion Type Definition | Integration: `tests/test_exe_009_pending_assertion.rs` — 13 tests: one per height/time variant (4 `ASSERT_*`, 4 `ASSERT_BEFORE_*`) with coin_id capture; `None` for `CREATE_COIN` and `ASSERT_EPHEMERAL`; bincode round-trip for struct and every `AssertionKind` variant; integration with `ExecutionResult::pending_assertions` (EXE-008). |
